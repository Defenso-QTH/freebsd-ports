--- src/venus/vkr_physical_device.h.orig
+++ src/venus/vkr_physical_device.h
@@ -31,6 +31,11 @@
    bool EXT_external_memory_metal;
    bool EXT_metal_objects;
 
+   /* VK_EXT_external_memory_host: import shm as host-visible VkDeviceMemory
+    * (FreeBSD path -- dma_buf mmap is unusable there, see the Defenso notes). */
+   bool EXT_external_memory_host;
+   uint64_t min_imported_host_pointer_alignment;
+
    VkPhysicalDeviceMemoryProperties memory_properties;
    VkPhysicalDeviceIDProperties id_properties;
    bool is_dma_buf_fd_export_supported;
